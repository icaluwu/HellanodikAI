# HellanodikAI — Base Requirements
# Shared dependencies across all environments

# Config & env
python-dotenv>=1.0.0

# Data processing
pdfplumber>=0.11.0
PyMuPDF>=1.24.0          # fallback PDF extraction (fitz)
tqdm>=4.66.0

# RAG — Retrieval
rank-bm25>=0.2.2
faiss-cpu>=1.8.0          # CPU-only FAISS (jangan pakai faiss-gpu di lokal)
sentence-transformers>=3.0.0   # embedding + CrossEncoder reranker

# RAG — General
numpy>=1.26.0
scipy>=1.13.0

# Logging & utils
loguru>=0.7.0
